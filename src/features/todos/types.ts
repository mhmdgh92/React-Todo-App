export type Todo = {
    id: string
    title: string
    completed: boolean
}

export type TodoRow = {
    id: string
    user_id: string
    title: string
    completed: boolean
    inserted_at: string
    updated_at: string
}